import { App, Plugin, PluginSettingTab, Setting } from 'obsidian';

export default class AstrBotConnectPlugin extends Plugin {
	async onload() {
		// Plugin initialization — WebSocket server will be added in subsequent tasks
	}

	async onunload() {
		// Cleanup
	}
}
